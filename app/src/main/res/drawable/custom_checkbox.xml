<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Checked state -->
    <item android:state_checked="true" android:drawable="@drawable/tick"/>

    <!-- Unchecked state -->
    <item android:drawable="@drawable/untick"/>

</selector>